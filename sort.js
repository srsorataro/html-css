const  livros = require('./filtrar')

function quickSort(array, esqueda, direita){
    if(array.length > 1) {
        let varTemp =  particionar(array,esquerda,direita)
    }
    function particionar(array, esquerda, direita){
    let pivo = array [Math.floor((esquerda + direita) / 2)]
    
}

    return array
}
console.log( quickSort   (livros,0, livros.length - 1))