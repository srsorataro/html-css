const  livros  = require("./filtrar")
const menorValor = require("./rapaz")

for(let i =  0; i < livros.length; i++){
    let menor = menorValor(livros,i)
    let livroAtual = livros[i]
    console.log(livroAtua)
}
